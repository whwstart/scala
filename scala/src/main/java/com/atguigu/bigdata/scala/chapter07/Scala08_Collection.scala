package com.atguigu.bigdata.scala.chapter07

object Scala08_Collection {

    def main(args: Array[String]): Unit = {

        // TODO 集合 - 排序
        val map = Map(
            ("a", 1), ("b", 2), ("c", 3)
        )

        // Map : K <-> V
        // List : KV

//        val list1: List[(String, Int)] = map.toList
//
//
//        val list = List(
//            (1,1), (2, 2), (3,3)
//        )
        //val map1: Map[Int, Int] = list.toMap
        //list.so

    }
}
